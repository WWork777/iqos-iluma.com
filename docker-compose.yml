version: "3"

services:
  app:
    build: .
    container_name: iqos-iluma-com-app
    restart: always
    environment:
      - NODE_ENV=production
      - VIRTUAL_HOST=iqos-iluma.com, www.iqos-iluma.com
      - LETSENCRYPT_HOST=iqos-iluma.com, www.iqos-iluma.com
      - LETSENCRYPT_EMAIL=parsh@gmail.com
      - REDIRECT_www.iqos-iluma.com=iqos-iluma.com
    networks:
      - web
    volumes:
      # Все сайты используют одну и ту же папку с картинками
      - /var/www/iluma-store.ru/images:/app/public/images:ro
      # - /var/www/iqos-iluma.com/.next:/app/.next

networks:
  web:
    external: true
